# React Native's Metro bundler cannot follow pnpm's symlinked store reliably, so
# this workspace uses a hoisted layout. This is Expo's documented setting for
# pnpm monorepos — without it, transitive imports inside expo-router and friends
# fail to resolve at bundle time.
node-linker=hoisted
